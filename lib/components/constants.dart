import 'package:flutter/material.dart';

const Color primaryColorAccent = Colors.redAccent;
const Color primaryColor = Colors.red;
const Color darkColor = Color.fromARGB(255, 39, 39, 39);

const horizantelSpacing = SizedBox(
  width: 3,
);

const verticalSpacing = SizedBox(
  height: 30,
);
